const Event = () => {
    //イベント表示(丸山)
    return (
        <div>
        <h1>{props.props.eventArray[props.props.eventNum]}</h1>
        {/* 丸山くん イベントをここで表示 */
       
        }
        </div>
    );
}

export default Event;